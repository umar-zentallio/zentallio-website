# Secrets — never commit. Keys live in a local .env (dev) or /etc/zentallio/api.env (server).
.env
.env.*
*.env

# Dependencies / OS / editor cruft
node_modules/
.DS_Store
*.log
__pycache__/

# audit harness -- tools/audit_mobile.py isay chalate waqt banata aur hata deta hai
_audit.html
